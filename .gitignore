# Secrets / données perso — ne jamais committer
.env
profil.json          # profil candidat réel (données personnelles)
profil.barman.json   # idem (version barman)
# Photos perso (le logo reste versionné)
assets/photo-*.jpg
assets/photo-*.png

# Fichiers générés
offres_24h.csv
offres_24h.json
*.csv
*.json
*.pdf                # CV générés
out/

# …mais on versionne les gabarits d'exemple
!profil.example.json

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
